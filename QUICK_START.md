# 🚀 Быстрый старт MTUCI Guesser

## Установка зависимостей

### 1. Сервер
```bash
cd server
npm install
```

### 2. Клиент
```bash
cd client
npm install
```

## Запуск приложения

Нужно запустить **ОБА** сервера в разных терминалах:

### Терминал 1 - Бэкенд
```bash
cd server
npm run dev
```
Сервер запустится на **http://localhost:3001**

### Терминал 2 - Фронтенд
```bash
cd client
npm run dev
```
Клиент запустится на **http://localhost:5173**

## Открыть приложение

Откройте в браузере: **http://localhost:5173**

## Структура портов

- **Frontend**: http://localhost:5173 (Vite dev server)
- **Backend API**: http://localhost:3001 (Express server)
- **Загруженные файлы**: http://localhost:3001/uploads/

## Первый запуск

1. Откройте приложение в браузере
2. Создайте аккаунт (необязательно, можно играть без регистрации)
3. Нажмите "Начать игру"
4. **Важно**: Сначала нужно добавить этажи и локации через админ панель!

## Добавление данных (Админ)

Войдите как администратор (`admin@admin.com` / `admin`) и нажмите кнопку "Админ панель" на главной странице:

1. **Добавить этаж**:
   - Загрузите карту этажа (PNG/JPG)
   - Укажите здание и уровень
   - Нажмите "Create Floor"

2. **Добавить локацию**:
   - Выберите этаж
   - Загрузите фотографию места
   - Кликните на карте где находится это место
   - Нажмите "Create Location"

## Возможные проблемы

### Ошибка "Cannot find package 'express-session'"
```bash
cd server
npm install express-session bcrypt
```

### Ошибка CORS
Убедитесь что:
- Сервер запущен на порту 3001
- Клиент запущен на порту 5173
- В `server/src/index.js` указан правильный origin: `http://localhost:5173`

### База данных не создается
База данных SQLite создается автоматически при первом запуске сервера в папке:
```
server/data/geoguesser.sqlite
```

### Загруженные файлы не отображаются
Проверьте что папка `server/uploads/` создана и доступна

## Структура БД

После запуска сервера автоматически создаются таблицы:
- `users` - пользователи
- `floors` - этажи/карты
- `locations` - локации для угадывания
- `game_results` - результаты игр

## Полезные команды

```bash
# Перезапустить сервер (если запущен через nodemon)
rs

# Очистить базу данных
rm server/data/geoguesser.sqlite

# Очистить загруженные файлы
rm -rf server/uploads/*

# Установить все зависимости сразу (из корня)
npm install --prefix server && npm install --prefix client
```

## Технологии

- **Frontend**: React + TypeScript + Vite
- **Backend**: Express.js + better-sqlite3
- **Auth**: bcrypt + express-session
- **Styling**: Custom CSS (Figma design)

## Следующие шаги

1. Добавьте несколько этажей через админку
2. Добавьте по 3-5 локаций для каждого этажа
3. Создайте аккаунт
4. Сыграйте игру!
5. Проверьте таблицу лидеров

---

**Приятной игры!** 🎮

